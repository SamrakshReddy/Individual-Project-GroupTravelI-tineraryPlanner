# React Frontend

Modern React + Vite frontend for the existing Group Travel Itinerary Planner backend.

## Setup

```bash
cd Frontend
npm install
npm run dev
```

The app calls the existing backend at `http://localhost:5000/api` by default. To change it, copy `.env.example` to `.env` and update `VITE_API_URL`.
